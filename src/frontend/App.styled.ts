import styled from '@emotion/styled'

export const AppContainer = styled.div`
    display: flex;
    flex-flow: column;
    height: 100vh;
`
export const AppContentPanel = styled.div`
    height: 100%;
    display: flex;
    flex-flow: row;
    overflow-y: auto;
`
export const AppContentMainPanel = styled.div`
    width: 100%;
    overflow-y: auto;
`
